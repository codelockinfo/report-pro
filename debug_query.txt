Dataset: inventory_pending_fulfillment
Query:
query { products(first: 250) { edges { node { id title variants(first: 200) { edges { node { id title sku price inventoryPolicy inventoryQuantity image { url } inventoryItem { id unitCost { amount currencyCode } inventoryLevels(first: 50) { edges { node { quantities(names: ["committed"]) { name quantity } } } } } } } } createdAt updatedAt status vendor productType } } } }
