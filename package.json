{
  "name": "report-pro",
  "version": "1.0.0",
  "description": "Shopify Reports App - Better Reports alternative",
  "main": "server/dist/server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd web && npm run dev",
    "build": "npm run build:server && npm run build:client",
    "build:server": "cd server && npm install && npm run build",
    "build:client": "cd web && npm install && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../web && npm install",
    "prebuild": "npm run install:all",
    "setup:env": "node setup-env.js",
    "test": "cd server && npm test"
  },
  "keywords": ["shopify", "reports", "analytics", "dashboard"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
